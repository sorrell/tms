export enum TemperatureUnit {
    Fahrenheit = 'F',
    Celsius = 'C',
}

export enum StopType {
    Pickup = 'pickup',
    Delivery = 'delivery',
}

export enum Notable {
    Shipment = 'shipment',
    Customer = 'customer',
}

export enum Contactable {
    Shipment = 'shipment',
    Customer = 'customer',
}
